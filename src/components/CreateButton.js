import { Button } from "@mui/material";

function CreateButton() {
    return (
        <Button
            fullWidth
            variant="outlined"
            color="success"
            style={{ marginTop: "20px" }}
        >
            CREATE
        </Button>
    );
}

export default CreateButton;
